#include <bits/stdc++.h>
#define FIN ios::sync_with_stdio(0);cin.tie(0);cout.tie(0)
using namespace std;
typedef long long ll;
#include <list>

class Triangule{
    public:
    int x = 1;
    list<int> list{0};
};

Triangule generate_triangule(x, previous){
    int previous_sum, x_triangule;
    list<int> x_list;
    Triangule triangule;
    
}

int main(){

}